workflows:
  # ios-method-1:
  #   name: IOS signing method-1 workflow
  #   environment:
  #     ios_signing:
  #       distribution_type: app_store
  #       bundle_identifier: br.com.profair.profair
  #   scripts:
  #     - script: keychain initialize
  #     - script: keychain add-certificates

  #     - script: xcode-project use-profiles
  #     - script: flutter packages pub get
  #     - name: Flutter build ipa and automatic versioning
  #       script: flutter build ipa --export-options-plist=/Users/builder/export_options.plist
  #   artifacts:
  #     - build/ios/ipa/*.ipa
  #   publishing:
  #     email:
  #       recipients:
  #         - arbian.app@gmail.com
  # ios-method-2:
  #   name: IOS signing method-2 workflow
  #   environment:
  #     ios_signing:
  #       provisioning_profiles:
  #         - profair_certificate
  #       certificates:
  #         - profair_certificate_distribution
  #   scripts:
  #     - script: keychain initialize
  #     - script: keychain add-certificates

  #     - script: xcode-project use-profiles
  #     - script: flutter packages pub get
  #     - name: Flutter build ipa and automatic versioning
  #       script: flutter build ipa --export-options-plist=/Users/builder/export_options.plist
  #   artifacts:
  #     - build/ios/ipa/*.ipa
  #   publishing:
  #     email:
  #       recipients:
  #         - arbian.app@gmail.com
  app-store:
    name: Publishing to App Store workflow
    environment:
      ios_signing:
        provisioning_profiles:
          - profair_certificate
        certificates:
          - profair_certificate_distribution
    integrations:
      app_store_connect: api key profair
    scripts:
      - script: xcode-project use-profiles
      - script: flutter packages pub get
      - name: Flutter build ipa
        script: flutter build ipa --export-options-plist=/Users/builder/export_options.plist
    artifacts:
      - build/ios/ipa/*.ipa
    publishing:
      app_store_connect:
        auth: integration
        submit_to_app_store: true
        release_type: SCHEDULED
        copyright: 2023 Profair


  android:
    name: Android workflow
    environment:
      android_signing:
        - profair_keystore_android
    scripts:
      - name: Flutter Clean
        script: flutter clean
      - name: Get Flutter packages
        script: flutter packages pub get
      - name: Build apk
        # script: flutter build apk --release
        script: flutter build appbundle --release
      - name: Set up key properties
        script: cat >> "CM_BUILD_DIR/android/key.properties" <<EOF storePassword=$CM_KEYSTORE_PASSWORD keyPassword=$CM_KEY_PASSWORD $keyAlias=$CM_KEY_ALIAS storeFile=$CM_KEYSTORE_PATH EOF
    artifacts:
      - build/**/outputs/apk/**/*.apk
      - build/**/outputs/**/*.aab